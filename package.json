{
  "name": "crm-whatsapp",
  "version": "0.1.0",
  "type": "module",
  "description": "CRM multi-tenant integrado con WhatsApp Cloud API (Meta) - chatbot ValleyTech",
  "main": "src/server.js",
  "scripts": {
  "dev": "node --watch src/server.js",
  "start": "npx prisma migrate deploy && node src/server.js",
  "postinstall": "prisma generate",
  "prisma:generate": "prisma generate",
  "prisma:migrate": "prisma migrate dev",
  "prisma:studio": "prisma studio"
},
"engines": {
  "node": ">=20.0.0"
},
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "axios": "^1.7.7",
    "bullmq": "^5.12.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "helmet": "^7.1.0",
    "ioredis": "^5.4.1",
    "pino": "^9.4.0",
    "pino-http": "^10.3.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "prisma": "^5.20.0"
  }
}
